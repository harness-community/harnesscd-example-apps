apiVersion: v2
name: demo-app
description: >-
  Minimal demo chart for the Kustomize post-renderer example.
  This chart intentionally has NO label placeholders — labels are
  injected externally by the post-renderer, proving zero chart changes are needed.
type: application
version: 0.1.0
appVersion: "1.0"
